[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd api && npm install && cd .. && npm run build"

[deploy]
startCommand = "NODE_ENV=production cd api && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
